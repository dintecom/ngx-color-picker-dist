export interface IColorPickerConfig {
    indicatorTitle: string;
    presetsTitle: string;
}
export declare class ColorPickerConfig implements IColorPickerConfig {
    indicatorTitle: string;
    presetsTitle: string;
}
